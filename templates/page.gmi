# {{ self.title }}

{{ self.body }}

%% if let Some(meta) = self.metadata {
%%   if let Some(incoming) = meta.incoming {
incoming:
%%     for (path, title) in incoming {
=> {{ utf8_percent_encode(path, URL_PATH_UNSAFE) }} {{ title }}
%%     }
%%   }
%% }
